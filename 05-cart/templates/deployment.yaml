apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart
  namespace: roboshop
  labels:
    component: cart
    project: roboshop
    tier: app
spec:
  replicas: 1
  selector:
    matchLabels:
      component: cart
      project: roboshop
      tier: app
  template:
    metadata:
      labels:
        component: cart
        project: roboshop
        tier: app
    spec:
      containers:
      - name: cart
        image: praneeth2701/cart:v2
        resources:
          requests:
            cpu: "50m"
            memory: "128Mi"
          limits:
            cpu: "128m"
            memory: "256Mi"
        envFrom:
        - configMapRef:
            name: cart
